# 导航页创建指南

## 目标

根据当前目录下所有符合 `*/index.html` 模式的功能目录，自动创建一个站点导航页。导航页用于集中展示并跳转到各个功能页面。

网站部署后，每个功能的访问路径为：

```text
/xxxx/
```

导航页中的跳转链接必须使用相对路径，例如：

```html
<a href="./xxxx/">功能名称</a>
```

## 目录扫描规则

1. 以当前目录为根目录进行扫描。
2. 自动查找所有符合 `*/index.html` 模式的一级子目录。
3. 不写死任何具体目录名，目录数量和目录名称都应由扫描结果决定。
4. 符合条件的通用目录结构：

```text
当前目录/
  任意目录名/
    index.html
  另一个目录名/
    index.html
```

5. 每个匹配目录都生成一个对应的导航路径：

```text
./任意目录名/
./另一个目录名/
```

6. 不应把普通资源目录作为功能展示，例如仅存放图片、样式、脚本、数据的目录，除非该目录下存在可访问的 `index.html`。
6. 不要扫描tags 目录

## 每个功能卡片内容

每个功能以一个卡片展示，卡片必须包含以下信息：

1. **名称**
   - 使用功能目录名或从页面标题中提取更友好的名称。
   - 名称需要作为超链接。
   - 链接使用相对路径，例如 `./xxxx/`。

2. **功能概述**
   - 简要说明该功能用途。
   - 不超过 30 个汉字。
   - 如果无法准确识别功能，可根据目录名和页面标题生成概述。

3. **UI 展示**
   - 使用清晰、现代、适合导航的卡片式布局。
   - 卡片需要有明显的可点击状态。
   - 页面应支持桌面端和移动端自适应。

## 推荐 UI 要求

导航页应简洁、易扫读，并适合静态站点首页使用。

建议包含：

1. 顶部标题，例如“功能导航”。
2. 简短副标题，说明这是当前站点的功能入口。
3. 响应式卡片网格：
   - 桌面端每行 3 到 4 个卡片。
   - 平板端每行 2 个卡片。
   - 手机端每行 1 个卡片。
4. 每张卡片包含：
   - 功能名称。
   - 不超过 30 个汉字的功能概述。
   - 一个可点击的相对路径链接。
5. 卡片 hover 状态：
   - 轻微上移。
   - 边框或阴影增强。
   - 链接状态清晰。
6. 文字和按钮不得溢出容器。
7. 整体视觉应克制、清楚、实用，不使用过度装饰。

## 生成导航页时的建议流程

1. 扫描当前目录下所有一级子目录，匹配 `*/index.html`。
2. 从匹配结果中提取目录名。
3. 对每个匹配目录生成一个功能卡片。
4. 卡片链接统一使用：

```text
./目录名/
```

5. 功能名称优先级：
   - 优先使用 `index.html` 中的 `<title>`。
   - 如果没有标题，使用目录名。
6. 功能概述优先级：
   - 优先根据页面内容、标题、目录名概括。
   - 概述必须控制在 30 个汉字以内。
7. 生成或更新根目录下的 `index.html`，作为导航首页。

## 示例卡片数据结构

```json
[
  {
    "name": "功能名称",
    "href": "./目录名/",
    "summary": "不超过30个汉字的功能概述"
  },
  {
    "name": "另一个功能名称",
    "href": "./另一个目录名/",
    "summary": "不超过30个汉字的功能概述"
  }
]
```

## 示例 HTML 结构

```html
<main class="page">
  <header class="hero">
    <h1>功能导航</h1>
    <p>当前站点的功能入口。</p>
  </header>

  <section class="grid" aria-label="功能列表">
    <a class="card" href="./目录名/">
      <strong>功能名称</strong>
      <span>不超过30个汉字的功能概述</span>
    </a>
  </section>
</main>
```

## 交付要求

最终导航页应满足：

1. 所有功能卡片都能正确跳转到 `/xxxx/` 对应页面。
2. 页面中的链接使用相对路径 `./xxxx/`。
3. 每个功能都有名称和不超过 30 个汉字的概述。
4. UI 在桌面端和移动端都清晰可用。
5. 不展示没有 `index.html` 的资源目录。
